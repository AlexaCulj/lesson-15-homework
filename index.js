
var a = prompt("Enter a");
var b = prompt("Enter b");
var c = prompt("Enter c");

function calcdiscrim (a, b, c) {
    
    return b * b - 4 * a * c;
}
function calculateRoots (x, x1, x2) {
    
    if (discrim < 0) {
        return x = " Нет действительных корней";

    } else if (discrim = 0) {
        return x = -b / 2 * a;
        
     } else (discrim > 0); {
         return x1,x2 = -b +- Math.sqrt(discrim) / 2 * a;
        
     }
}
